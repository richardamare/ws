# Diagrams

Mermaid (`.mmd`) diagrams. GitHub renders them in-place.

- **`system-context.mmd`** — how `ws` sits between the user, cmux, the two Azure identities
  (Reader SP for sessions, personal admin for deliberate write), optional Docker, and local config.
